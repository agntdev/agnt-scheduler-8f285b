[
  {
    "name": "My bookings shows empty state when no bookings exist",
    "steps": [
      {
        "send": { "callback": "booking:manage" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "No bookings yet — tap 📅 Book service to schedule one." } }
        ]
      }
    ]
  }
]

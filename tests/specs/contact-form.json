[
  {
    "name": "Contact form opens support prompt",
    "steps": [
      {
        "send": { "callback": "contact:form" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "How can we help? Type your message below and tap Send." } }
        ]
      }
    ]
  }
]
